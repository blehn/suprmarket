- unless current_user 
  = link_to t(:register), sign_up_path
  = link_to t(:login), sign_in_path
- else
  Market
  = render 'shared/search'
  = current_user.username
  = link_to t(:new_listing), new_listing_path
  = link_to t(:dashboard), dashboard_path
  = link_to t(:sign_out), sign_out_path
